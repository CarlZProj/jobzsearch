const {StyleSheet} = require('react-native');

const styles = StyleSheet.create({
  fieldContainer: {marginTop: 30, backgroundColor: '#FDD7E4'},
  textContainer: {borderWidth: 2, borderColor: '#000000'},
  inputContainer: {borderWidth: 2, borderColor: '#000000'},
  submitContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  submitButton: {
    borderWidth: 2,
    borderColor: '123123',
    width: 100,
  },
  submitText: {
    textAlign: 'center',
  },
});

export default styles;
